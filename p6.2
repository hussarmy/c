// find the the greatest value
#include <stdio.h>

void main() {
    int a,b,c;// Write C code here
    printf("enter the vale of a:");
    scanf("%d",&a);
    printf("enter value of b:");
    scanf("%d",&b);
    printf("enter value of c:");
    scanf("%d",&c);
    if(a>b&&a>c)
    {
        printf("a is greatest");
    }
   if(b>a)
   {
       printf("b is greatest");

   }
   else
   printf("c is greatest");
}
